name : manual_workflow

on:
  workflow_dispatch:
   inputs:
     name: 
      description: "Name"
      type: string
      default: 'test'

jobs:
  demo1:
   runs-on: ubuntu-latest

   steps:
     - name: Greetings
       run: echo "${{inputs.name}}"
  
  
